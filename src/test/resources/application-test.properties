#spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:for_testing}
spring.datasource.username=${DB_USER:for_testing}
spring.datasource.password=${DB_PASSWORD:root}

spring.flyway.enabled=false
spring.flyway.url=jdbc:postgresql://localhost:5432/for_testing
spring.flyway.user=for_testing
spring.flyway.locations=classpath:/db/migration
spring.flyway.password=root