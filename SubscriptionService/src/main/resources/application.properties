spring.application.name=SubscriptionService
server.port=7092

# ???? ??????
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:subs_db}
spring.datasource.username=${DB_USER:chief}
spring.datasource.password=${DB_PASSWORD:root}

# kafka
spring.kafka.bootstrap-servers=kafka:9092
spring.kafka.consumer.group-id=subs-group
spring.kafka.consumer.auto-offset-reset=earliest